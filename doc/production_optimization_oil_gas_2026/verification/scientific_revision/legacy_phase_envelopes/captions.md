## ch02_thermodynamic_foundations

NeqSim SRK vapour-liquid saturation envelope for the explicitly specified fluid in Section 2.6.3. The traced maxima are 83.8 °C (cricondentherm) and 154.7 bara (cricondenbar). Markers are sampled continuation extrema; no critical point or production/export trajectory is inferred. Dew/bubble assignment was checked by fresh TP flashes at three pressures around each branch. Continuation gaps remain open; solids, hydrates and aqueous stability are outside this VLE calculation.

Input amounts: methane=0.8; ethane=0.08; propane=0.05; n-butane=0.03; n-pentane=0.02; n-heptane=0.02. Amount sum 1, normalized as recorded in the evidence JSON.

## ch03_fluid_characterization

NeqSim PR vapour-liquid saturation envelope for an illustration-only defined-compound surrogate, not the Section 3.7.5 TBP fluid. The traced maxima are 83.5 °C (cricondentherm) and 138.8 bara (cricondenbar). Markers are sampled continuation extrema; no critical point or production/export trajectory is inferred. Dew/bubble assignment was checked by fresh TP flashes at three pressures around each branch. Continuation gaps remain open; solids, hydrates and aqueous stability are outside this VLE calculation.

Input amounts: methane=70; ethane=10; propane=8; n-butane=5; n-pentane=4; n-hexane=3. Amount sum 100, normalized as recorded in the evidence JSON.

## ch12_gas_processing

NeqSim SRK vapour-liquid saturation envelope for the explicitly specified fluid in Section 12.8. The traced maxima are 93.8 °C (cricondentherm) and 168.7 bara (cricondenbar). Markers are sampled continuation extrema; no critical point or production/export trajectory is inferred. Dew/bubble assignment was checked by fresh TP flashes at three pressures around each branch. Continuation gaps remain open; solids, hydrates and aqueous stability are outside this VLE calculation.

Input amounts: nitrogen=1; CO2=2.5; methane=80; ethane=6; propane=4; i-butane=1; n-butane=2; i-pentane=0.5; n-pentane=0.5; n-hexane=1; n-heptane=0.5; n-octane=1. Amount sum 100, normalized as recorded in the evidence JSON.

## ch19_export_and_metering

NeqSim SRK vapour-liquid saturation envelope for the explicitly specified fluid in Section 19.6.2. The traced maxima are 27.3 °C (cricondentherm) and 111.0 bara (cricondenbar). Markers are sampled continuation extrema; no critical point or production/export trajectory is inferred. Dew/bubble assignment was checked by fresh TP flashes at three pressures around each branch. Continuation gaps remain open; solids, hydrates and aqueous stability are outside this VLE calculation.

Input amounts: nitrogen=0.008; CO2=0.015; methane=0.88; ethane=0.055; propane=0.02; i-butane=0.005; n-butane=0.007; i-pentane=0.003; n-pentane=0.002; n-hexane=0.003; n-heptane=0.002. Amount sum 1, normalized as recorded in the evidence JSON.
